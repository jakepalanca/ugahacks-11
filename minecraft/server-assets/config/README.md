Put Paper/Bukkit server config files here before running:

`/Users/jake/hunyuan3d-batch/minecraft/ec2/push-assets-to-s3.sh <bucket> <prefix>`

Everything in this folder syncs to the server root (`/opt/minecraft/server/`).
