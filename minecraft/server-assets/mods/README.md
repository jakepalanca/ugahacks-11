Legacy fallback path.

If `server-assets/custom/` is missing, the upload script falls back to this directory and syncs it to:

- `/opt/minecraft/server/custom/`

Prefer placing new files in `server-assets/custom/`.
